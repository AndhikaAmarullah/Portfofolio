runtime: python37

handlers:
- url: /styles
  static_dir: styles
  
- url: /images/(.*\.(gif|png|jpg))
  static_files: images/\1
  upload: images/(.*\.(gif|png|jpg))
  
- url: /.*
  static_files: index.html
  script: index.html
